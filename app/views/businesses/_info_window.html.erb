  <%= image_tag(business.image_url, alt: business.name, style: "width: 100%; height: 100px; box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);  ") if business.image_url.present? %>

<div class="carte-info">
  <% @basket = Basket.where(business_id: business.id).first%>
   <h2><a href="<%= basket_path(@basket.id) %>">
      <%# carte business name %>

    <button id="btn-bussiness"><%= business.name%>
      </button>
      </a>
      </h2>
</div>
